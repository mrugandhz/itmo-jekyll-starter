# Custom site-wide data, in lieu of modifying (and risking conflict) with `_config.yml`

# Custom `author` property

author:
  name: &name Scarlet Hawk
  email: scarlet.hawk@iit.example.edu
  github: https://github.com/scarlet_hawk

# Replacement for `site.title`, from `_config.yml`
# Reference `site.data.custom.title` instead
title: *name

# For use with the navigation include in `_includes/`
navigation:
  - index.md
  - resume.md
  - about.md
